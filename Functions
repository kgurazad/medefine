Book/ View appointments from Patient end
Doctor's end, manage patients, view appointments, also stat track symptom scores(both doc and patient)
admin does everything

misc:
login system, admin login, db 
